# 回测回归测试规范

回测修改不能只检查接口是否返回成功。所有计算测试都使用固定、离线、可手工核算的输入，避免实时行情变化掩盖回归问题。

## 本地执行

只运行回测相关测试：

```powershell
.\test-backtest.ps1
```

运行完整后端回归：

```powershell
.\test-backtest.ps1 -Full
```

GitHub Actions 会在每次 push 和 pull request 上自动执行完整测试。失败时不得合并回测改动。

## 必须保持的计算规则

| 领域 | 回归保证 |
|---|---|
| 选股 | 阈值过滤、因子排序、行业配额、持仓数量和调仓日快照 |
| 时点 | PB/PE 分位不读取估值日之后的数据，分红只使用当时已公告且已实施的事件 |
| 收益 | 持仓权重自然漂移，不假设免费每日再平衡；缺失交易日沿用上一有效价格，不产生虚假收益 |
| 分红 | 银行不复权价格的除息跌幅与现金分红各计一次；跨行业后复权收益不重复叠加现金分红 |
| 交易成本 | 买卖双边佣金、滑点、过户费和卖出印花税按实际换手计算，首次建仓成本计入总收益 |
| 个股账本 | `持仓市值 = 成本 + 盈亏`，`总盈亏 = 价格盈亏 + 分红贡献` |
| 利润贡献 | 每年及全周期保留所有曾持有股票；`股票净贡献 + 现金收益 = 组合利润`，交易成本归属到实际买卖股票 |
| 组合账本 | 固定输入的净值、现金、权重、换手和总成本与手工计算结果一致 |
| 风险指标 | 总收益、年度收益、最大回撤、恢复日期、波动率、Sharpe 和 Calmar 使用同一净值口径 |
| 展示价格 | 回测收益可使用后复权价；建仓价、当前价、区间价格和价格路径必须使用真实不复权收盘价 |
| 基准 | 银行按股票等权；跨行业先行业内等权，再对行业等权 |

## 修改要求

修改以下任一部分时，必须先运行回测专项测试，再运行完整测试：

- `strategy_backtest.py` 或 `cross_industry_backtest.py`
- 回测请求/响应模型
- 行情、复权、分红或缓存读取逻辑
- 选股评分、权重、调仓、成本、收益、盈亏归因或风险指标
- 前端回测字段和图表口径

每个修复必须添加一个能在旧代码上失败、在新代码上通过的确定性用例。不能因为现有输出变化就直接更新期望值；必须先用独立公式解释差异。

## 边界说明

这些测试保证“给定输入后的计算正确且稳定”，不等于外部行情与财务数据天然正确。真实数据源还需要独立校验完整性、复权标识、公告时点、停牌/退市和股票池幸存者偏差。当前跨行业质量分包含公开的研究先验，银行历史回测也尚未拥有所有历史财务指标的逐公告日快照；涉及这些输入的结论必须保留相应限制说明。
